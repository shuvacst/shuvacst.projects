# PBR-and-ACL-Controlled-Network-Lab (পলিসি-ভিত্তিক রাউটিং এবং ACL দ্বারা নিয়ন্ত্রিত নেটওয়ার্ক ল্যাব)

## 🌐 সংক্ষিপ্ত বিবরণ (Overview)

এই রিপোজিটরিটি একটি অ্যাডভান্সড নেটওয়ার্কিং প্রজেক্ট, যা ফেইলওভার, রিডান্ডেন্সি এবং মাল্টি-লেয়ার সিকিউরিটি প্রয়োগ করে তৈরি করা হয়েছে। এটি মূলত একটি ছোট কর্পোরেট বা অফিসের নেটওয়ার্কের সিমুলেশন, যেখানে নির্ভরযোগ্য ইন্টারনেট সংযোগ এবং কঠোর অ্যাক্সেস কন্ট্রোল (Access Control) নিশ্চিত করা হয়েছে।

এই ল্যাবটির মাধ্যমে নিম্নলিখিত দক্ষতাগুলি স্পষ্টভাবে দেখানো হয়েছে:
* লিংক ফেইলওভারের জন্য **স্বয়ংক্রিয় ব্যবস্থা** তৈরি।
* ব্যবহারকারী এবং সার্ভিসের উপর ভিত্তি করে ট্র্যাফিকের পথ **নিয়ন্ত্রণ (Policy-Based Routing)**।
* গুরুত্বপূর্ণ রাউটার অ্যাক্সেস সুরক্ষিত করার জন্য **কঠোর নিরাপত্তা ব্যবস্থা (ACL & SSH)**।


## 💻 টপোলজি ডায়াগ্রাম

https://github.com/shuvacst/shuvacst.projects/blob/16adbf3885e685f10fe692a72f1ae1ef55e164e9/PBR-and-ACL-Controlled-Network-Lab/PBR-and-ACL-Controlled-Network-Lab.JPG




## ✨ এই প্রজেক্টের প্রধান কাজ এবং কেন এটি গুরুত্বপূর্ণ

এই ল্যাবটিতে একাধিক গুরুত্বপূর্ণ নেটওয়ার্কিং ও সিকিউরিটি কনফিগারেশন একীভূত করা হয়েছে:

### ১. স্বয়ংক্রিয় হাই অ্যাভেইলেবিলিটি (High Availability - HA)

* **দুটি ইন্টারনেট গেটওয়ে (Primary/Secondary):** আপনার নেটওয়ার্কে R1 এবং R2 এর মাধ্যমে দুটি আলাদা ইন্টারনেট সংযোগ রয়েছে। এর ফলে একটি সংযোগ ব্যর্থ হলেও, অন্যটি স্বয়ংক্রিয়ভাবে কাজ শুরু করে দেবে।
* **স্মার্ট ফেইলওভার (Smart Failover):** এই ল্যাব PBR (Policy-Based Routing) ব্যবহার করে। এর মানে হলো, R3 রাউটার সব সময় Primary Link (R1) সচল আছে কিনা তা ট্র্যাক করে। যদি R1 কোনো কারণে বন্ধ হয়ে যায়, তবে R3 দেরি না করে সমস্ত ট্র্যাফিক সেকেন্ডারি Link (R2) এর দিকে পাঠিয়ে দেয়।
* **অটো-রিকভারি:** শুধু ফেইলওভার নয়, যখন Primary Link (R1) আবার সচল হয়, তখন সমস্ত ট্র্যাফিক স্বয়ংক্রিয়ভাবে R1 এর দিকে ফিরে যায়। এটি নিশ্চিত করে যে আপনার নেটওয়ার্ক সবসময় দ্রুততম এবং পছন্দের পথটি ব্যবহার করবে।

### ২. অ্যাডভান্সড অ্যাক্সেস কন্ট্রোল (ACL Implementation)

* **ইন্টারনেট সুরক্ষা:** ACL ব্যবহার করে LAN-এর ব্যবহারকারীদের (যেমন PC1) ইন্টারনেট অ্যাক্সেস কঠোরভাবে নিয়ন্ত্রণ করা হয়েছে। শুধুমাত্র অনুমোদিত ট্র্যাফিকই বাইরে যেতে পারবে।
* **সার্ভিস-ভিত্তিক ব্লক:** PC2 কে রাউটার R2-এ পিং বা Telnet করার সুযোগ দেওয়া হয়নি। এটি প্রমাণ করে যে আপনি নির্দিষ্ট সার্ভিস বা প্রোটোকলের ভিত্তিতেও অ্যাক্সেস ব্লক করতে পারেন।
* **অভ্যন্তরীণ জোন নিয়ন্ত্রণ:** PC3-এর একটি ভিন্ন নিরাপত্তা প্রয়োজন থাকায়, ACL দ্বারা নিশ্চিত করা হয়েছে যে এটি PC1 এবং PC2-কে কোনোভাবেই পিং করতে পারবে না। এটি বিভিন্ন অভ্যন্তরীণ নেটওয়ার্ক জোনকে আলাদা রাখতে সাহায্য করে।

### ৩. নিরাপদ রিমোট ম্যানেজমেন্ট (SSH Security)

* **VTY লাইনের নিরাপত্তা:** রাউটার R3-এর অ্যাডমিনিস্ট্রেটিভ অ্যাক্সেস (SSH) সুরক্ষিত করতে VTY লাইন কনফিগার করা হয়েছে।
* **সোর্স-ভিত্তিক অ্যাক্সেস:** একটি বিশেষ অ্যাক্সেস রুল তৈরি করা হয়েছে, যা শুধুমাত্র R3 এর নিজস্ব LAN (172.16.1.0/24) থেকে আসা ব্যবহারকারীদেরকেই SSH লগইন করার অনুমতি দেয়। বাইরের কেউ এই রাউটারে অ্যাডমিন অ্যাক্সেস নিতে পারবে না।
* **SSH হার্ডেনিং:** SSH সংস্করণ 2 ব্যবহার এবং `ip domain name` ও `crypto key generate rsa` এর মাধ্যমে রাউটারের নিরাপত্তা বাড়ানো হয়েছে।

---

## 🛠️ টাস্ক এবং কনফিগারেশনের সারাংশ

এই ল্যাবে সফলভাবে বাস্তবায়িত প্রধান কনফিগারেশনগুলো হলো:

| কাজের বিভাগ | কনফিগারেশন (Configuration) |
| :--- | :--- |
| **PBR/HA** | **IP SLA Tracking** এবং **Route-Map** ব্যবহার করে লিংক ফেইলওভার লজিক। |
| **নিরাপত্তা** | SSH v2 এবং সোর্স-ভিত্তিক অ্যাক্সেস কন্ট্রোল লিস্ট দ্বারা VTY লাইন সুরক্ষা। |
| **ACL (ইন্টারনেট)** | নির্দিষ্ট PC এবং প্রোটোকলের উপর ভিত্তি করে ইন্টারনেট অ্যাক্সেস নিয়ন্ত্রণ। |
| **ACL (ইন্টারনাল)** | PC3 থেকে PC1/PC2 তে ICMP (পিং) ট্র্যাফিক ব্লক করা। |
| **নেটওয়ার্ক পরিষেবা** | **DHCP সার্ভার** এবং VLAN কনফিগারেশন বাস্তবায়ন। |

---

## ⬇️ সরাসরি ডাউনলোড লিঙ্ক (Direct Download)

এই প্রোজেক্টের সমস্ত কনফিগারেশন ফাইল এবং GNS3 টপোলজি নিচে দেওয়া লিঙ্ক থেকে ডাউনলোড করা যাবে:

[**Download PBR-and-ACL-Controlled-Network-Lab.zip**](https://github.com/shuvacst/shuvacst.projects/raw/refs/heads/main/PBR-and-ACL-Controlled-Network-Lab/PBR-and-ACL-Controlled-Network-Lab.zip)

---

## 🚀 ল্যাবটি কিভাবে চালাবেন (How to Run the Lab)

এই প্রোজেক্টটি চালানোর জন্য নির্দিষ্ট পরিবেশের প্রয়োজন। এটি **GNS3** এবং **GNS3 VM** ব্যবহার করে তৈরি করা হয়েছে এবং লোকাল কম্পিউটারে চালানোর জন্য এই সরঞ্জামগুলি থাকা আবশ্যক:

### পূর্বশর্ত (Prerequisites)

1.  **GNS3 ইনস্টলেশন:** আপনার সিস্টেমে **GNS3** অ্যাপ্লিকেশনটি ইনস্টল করা বাধ্যতামূলক।
2.  **GNS3 VM:** যেহেতু এই টপোলজিতে রাউটার ইমেজ (Cisco IOS/VIO) ব্যবহার করা হয়েছে এবং আপনি VM ব্যবহার করেছেন, তাই **GNS3 ভার্চুয়াল মেশিন** (যেমন VMware বা VirtualBox-এ) ইনস্টল এবং GNS3 অ্যাপ্লিকেশনের সাথে সঠিকভাবে সংযুক্ত থাকতে হবে।
3.  **ইমেজ:** আপনার GNS3 VM-এ ব্যবহৃত রাউটার এবং সুইচ ইমেজগুলি (IOS/IOL) ইনস্টল থাকতে হবে। ক্লায়েন্ট ডিভাইস (PC1, PC2, PC3) গুলি GNS3-এর ডিফল্ট VPCS ব্যবহার করে।

### স্থাপনের ধাপসমূহ (Installation Steps)

1.  উপরে দেওয়া লিঙ্ক থেকে `.zip` ফাইলটি ডাউনলোড করে একটি ফোল্ডারে এক্সট্র্যাক্ট করুন।
2.  GNS3 অ্যাপ্লিকেশনটি ওপেন করুন।
3.  `File > Open Project` মেনু থেকে এক্সট্র্যাক্ট করা ফোল্ডারের মধ্যে থাকা `.gns3project` ফাইলটি সিলেক্ট করুন।
4.  প্রোজেক্টটি লোড হওয়ার পরে, **GNS3 VM চালু করুন** এবং নিশ্চিত করুন যে টপোলজির সমস্ত ডিভাইস **সবুজ** রঙ দেখাচ্ছে (এর মানে সমস্ত ডিভাইস সফলভাবে চালু হয়েছে)।

### যাচাইকরণ (Verification)

* **DHCP যাচাই:** PC1, PC2, PC3-এ লগইন করে `ip` কমান্ড দিয়ে নিশ্চিত করুন যে তারা R3 DHCP সার্ভার থেকে IP অ্যাড্রেস পেয়েছে।
* **ফেইলওভার পরীক্ষা:** রাউটার R3 এবং R1 এর মধ্যবর্তী ইন্টারফেসটি `shutdown` কমান্ড দিয়ে বন্ধ করুন। তারপর PC থেকে R2 এর মাধ্যমে ইন্টারনেট পিং করে দেখুন। এরপর R1 এর ইন্টারফেস `no shutdown` করে দেখুন যে পিং আবার R1 এর দিকে ফিরে আসে কিনা।
* **সিকিউরিটি পরীক্ষা:** ACLগুলি কাজ করছে কিনা তা যাচাই করার জন্য PC2 থেকে R2 কে পিং করুন (ফেইল হবে) এবং PC3 থেকে PC1 কে পিং করুন (ফেইল হবে)।
